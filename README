1. Execute o Docker Compose:
```
docker-compose up -d
```

2. O Docker Compose iniciará dois serviços:
   - Ollama: executa o modelo DeepSeek
   - Python-client: executa seu script

3. A primeira vez que rodar, você precisará baixar o modelo DeepSeek:
```
docker exec -it examinis-ai ollama pull deepseek-r1:1.5b
```
